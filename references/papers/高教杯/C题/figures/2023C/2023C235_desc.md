# 2023C235 插图描述


## p05_img0.jpg (p.5, 191×483)

根据您的要求，对该学术论文插图的描述如下：

1. **图表类型**：其他（箱线图 / 箱形图）。
2. **传达的核心信息**：该图展示了某一组单一变量数据的统计分布特征，包括其中位数、四分位数、上下边缘，以及在数据上方（高数值区间）存在的大量离群值（异常值）。
3. **图表布局**：单轴布局。图表呈垂直方向单箱体排列，仅在左侧有一个纵向的数值坐标轴，横向没有坐标轴或分类标签。
4. **标注方式**：
   * **标题**：无标题。
   * **坐标轴**：左侧带有纵向数值坐标轴，刻度标签为100、200、300、400（文字方向逆时针旋转了90度）；无坐标轴名称或单位。
   * **图例**：无图例。
5. **水印内容**：无水印。


## p05_img1.jpg (p.5, 1011×463)

根据您的要求，对该学术论文插图的描述如下：

1. **图表类型**：折线图
2. **传达的核心信息**：展示了每周总销售额（Total sales per week）随时间（Week）的波动趋势，并通过黑色和绿色两条折线对比了两种数据走势（通常为实际值与预测/拟合值）的高度一致性。
3. **图表布局**：单轴布局（标准的单Y轴二维坐标系）
4. **标注方式**：
   * **标题**：无图表标题。
   * **坐标轴**：横轴（X轴）标签为“Week”，纵轴（Y轴）标签为“Total sales per week”（逆时针旋转90度标注）。
   * **图例**：无图例（图中未直接标明黑色和绿色折线分别代表的具体数据集）。
5. **水印内容**：无水印。


## p06_img0.jpg (p.6, 904×202)

根据您的要求，对该插图的描述如下：

1. **图表类型**：其他（属于统计分析软件的控制台文本输出结果）。
2. **传达的核心信息**：展示了对“bbb$花叶类”和“bbb$水生根茎类”两组数据进行渐近双样本K-S检验（Asymptotic two-sample Kolmogorov-Smirnov test）的分析结果，检验结果显示两组数据存在极显著差异（$p < 2.2 \times 10^{-16}$）。
3. **图表布局**：无图形坐标轴，呈纯文本多行排版布局。
4. **标注方式**：
   * **标题**：位于顶部，为检验方法名称 `Asymptotic two-sample Kolmogorov-Smirnov test`。
   * **数据源标注**：`data:  bbb$花叶类 and bbb$水生根茎类`。
   * **统计量标注**：包含统计量 $D = 0.88479$ 以及显著性 $p\text{-value} < 2.2\text{e-}16$。
   * **假设标注**：指定了备择假设类型为双侧检验 `alternative hypothesis: two-sided`。
   * **坐标轴与图例**：无坐标轴和图例。
5. **水印内容**：无。


## p07_img0.jpg (p.7, 1240×318)

针对您提供的学术论文插图，解析如下：

1. **图表类型**
   * **表格**（具体为R语言控制台输出的**相关系数矩阵**文本表格）。

2. **传达的核心信息**
   * 该图展示了六种蔬菜类别（花叶类、花菜类、水生根茎类、茄类、辣椒类、食用菌）之间的相关性系数矩阵，用于分析不同类别之间的关联程度。

3. **图表布局**
   * 采用**矩阵/表格布局**，呈现为一个6行6列的对称数值矩阵。

4. **标注方式**
   * **标题/指令**：顶部有一行紫色的R语言运行命令 `> cor(bbb)`。
   * **行列标签**：
     * **列名（表头）**：花叶类、花菜类、水生根茎类、茄类、辣椒类、食用菌。
     * **行名**：花叶类、花菜类、水生根茎类、茄类、辣椒类、食用菌。
   * **图例/坐标轴**：无传统的坐标轴和图例。

5. **水印内容**
   * 无水印。


## p07_img1.jpg (p.7, 1236×312)

根据您的要求，对该学术论文插图的描述如下：

1. **图表类型**：表格（具体为R语言控制台输出的**相关系数矩阵**）。
2. **传达的核心信息**：该图展示了六类食品/作物（花叶类、花菜类、水生根茎类、茄类、辣椒类、食用菌）之间的相关性分析结果（由R语言命令 `cor(bbbb)` 计算得出）。
3. **图表布局**：矩阵/表格布局（非传统坐标轴图表，由6行6列组成的对称矩阵结构）。
4. **标注方式**：
   * **标题**：顶部有R语言运行的代码指令 `> cor(bbbb)`。
   * **行/列标签**：行首和列首分别标注了六个对应类别的中文名称（花叶类、花菜类、水生根茎类、茄类、辣椒类、食用菌），用于指示矩阵的维度。
   * **图例**：无图例。
5. **水印内容**：无水印。


## p08_img0.jpg (p.8, 577×505)

基于您提供的图片，以下是对该学术论文插图的描述：

1. **图表类型**：
   * **热力图**（具体为相关性矩阵热力图）。

2. **传达的核心信息**：
   * 该图展示了茄类、花菜类、花叶类、辣椒类、食用菌和水生根茎类这六种蔬菜类别之间的相关性强弱关系。

3. **图表布局**：
   * **单panel**布局。图表呈对称的正方形矩阵结构，横轴和纵轴的类别一一对应。

4. **标注方式**：
   * **坐标轴**：
     * **纵轴（左侧）**和**横轴（上方）**均用红色字体标注了六个蔬菜类别：茄类、花菜类、花叶类、辣椒类、食用菌、水生根茎类。
   * **数值标注**：矩阵的每个单元格内都标注了具体的数值，代表两两类别之间的相关系数。
   * **图例**：右侧配有一个垂直的渐变色条（Colorbar），标注了从 -1 到 1 的数值刻度，用颜色的深浅（深蓝到深红）来表征相关系数的正负和大小。
   * **标题**：图表本身没有主标题。

5. **水印内容**：
   * 无水印。


## p08_img1.jpg (p.8, 558×503)

针对您提供的学术论文插图，以下是详细的描述：

1. **图表类型**：热力图（具体为相关性矩阵热力图）。
2. **传达的核心信息**：该图展示了六种蔬菜及食品类别（茄类、花菜类、花叶类、辣椒类、食用菌、水生根茎类）之间两两相关的系数强弱。
3. **图表布局**：单panel布局，由一个 6×6 的二维网格矩阵和右侧一个垂直排列的渐变色条（图例）组成。
4. **标注方式**：
   * **标题**：图表无主标题。
   * **坐标轴**：纵轴（位于左侧）和横轴（位于上方，文字垂直排列）的标签完全相同，均为红色的六个类别名称（“茄类”、“花菜类”、“花叶类”、“辣椒类”、“食用菌”、“水生根茎类”）。
   * **图例**：右侧设有颜色渐变条，数值范围为 -1 到 1，用于指示相关系数的大小（蓝色系表示正相关，红色系表示负相关，颜色越深相关性越强）。
   * **内部标注**：热力图的每个单元格内都清晰地标注了具体的相关系数值，对角线上的值均为 1（代表自身与自身的相关性）。
5. **水印内容**：无水印。


## p09_img0.jpg (p.9, 627×253)

这是一幅关于学术论文插图的描述：

1. **图表类型**：折线图（时间序列图）。
2. **传达的核心信息**：该图展示了每周总销售量随时间（周数）变化的波动趋势，呈现出明显的周期性/季节性起伏。
3. **图表布局**：单panel布局（标准二维单轴布局），包含单一的横轴和纵轴。
4. **标注方式**：
   * **标题**：无。
   * **坐标轴**：横轴（X轴）标签为“Week”（周数）；纵轴（Y轴）标签为“Total sales per week”（每周总销售量）。
   * **图例**：无。
5. **水印内容**：无。


## p10_img0.jpg (p.10, 576×256)

针对您提供的学术论文插图，解析如下：

1. **图表类型**：
   其他（具体为**自相关函数图**，亦称 **ACF图** 或 **针状图/棒状图**）。

2. **传达的核心信息**：
   该图展示了自相关系数（ACF）随着滞后量（Lag）的增加呈现逐渐衰减并转为负值的趋势，用以分析时间序列数据的自相关性及其显著性。

3. **图表布局**：
   单Panel布局，采用单X轴与单Y轴的二维坐标系。

4. **标注方式**：
   * **标题**：无标题。
   * **坐标轴**：X轴标签为“Lag”（滞后量），Y轴标签为“ACF”（自相关系数，逆时针旋转90度显示）。
   * **图例**：无。
   * **其他标注**：
     * Y=0 处有一条黑色水平实线作为基准线。
     * 上下对称分布有两条蓝色水平虚线，通常用于表示自相关系数的显著性边界（置信区间）。

5. **水印内容**：
   无水印。


## p10_img1.jpg (p.10, 601×272)

这是一张学术论文插图的详细描述：

1. **图表类型**
   * **其他（偏自相关图 / PACF图）**：这是时间序列分析中常用的偏自相关函数（Partial Autocorrelation Function, PACF）图，表现为从零水平线出发的一系列垂直黑色实线段（针状图/脉冲图）。

2. **传达的核心信息**
   * 该图展示了时间序列在不同滞后阶数（Lag）下的偏自相关系数（Partial ACF），用于评估各阶滞后对当前值的影响是否具有统计显著性（即线段是否超出了蓝色虚线表示的置信区间边界）。

3. **图表布局**
   * **单图（单panel）布局**：采用单坐标轴系统。图中包含一条 $y=0$ 的灰色水平参考线，以及对称分布在零线上下两侧的两条蓝色平行虚线（代表显著性阈值/置信区间）。

4. **标注方式**
   * **横坐标轴（X轴）**：标签为 `Lag`（滞后），带有数值刻度。
   * **纵坐标轴（Y轴）**：标签为 `Partial ACF`（偏自相关系数），带有数值刻度。
   * **标题与图例**：无主标题，无图例。

5. **水印内容**
   * 无水印。


## p10_img2.jpg (p.10, 601×251)

基于您提供的学术论文插图，以下是详细描述：

1. **图表类型**
   * **其他（代码/文本输出）**：该图为统计分析软件（如R语言）中时间序列模型拟合结果的纯文本控制台输出。

2. **传达的核心信息**
   * 该图展示了对时间序列数据（`myts`）拟合 `ARIMA(0,0,1)(0,1,1)[52] with drift` 模型后的详细统计结果，包括模型系数、标准误差、拟合优度指标（如AIC、BIC）以及训练集的预测误差度量。

3. **图表布局**
   * **无图形轴布局**：不属于单轴/双轴/多panel。布局为自上而下的纯文本结构，分为模型基本信息、系数表（Coefficients）、模型统计量（sigma^2、似然值、AIC等）以及训练集误差度量表（Training set error measures）四个部分。

4. **标注方式**
   * **标题与标签**：
     * 顶部标有数据源及模型结构：`Series: myts` 和 `ARIMA(0,0,1)(0,1,1)[52] with drift`。
     * 中部设有系数表的列标签（`ma1`, `sma1`, `drift`）和行标签（`s.e.`，代表标准误差）。
     * 底部设有误差度量表的标题（`Training set error measures:`）和各列指标缩写（`ME`, `RMSE`, `MAE`, `MPE`, `MAPE`, `MASE`, `ACF1`）作为表头。
   * **无坐标轴与图例**（该文本输出不包含传统的图表坐标轴和图例）。

5. **水印内容**
   * 无水印。


## p11_img0.jpg (p.11, 638×240)

这幅学术论文插图的描述如下：

1. **图表类型**：折线图（时间序列图）。
2. **传达的核心信息**：该图展示了每周总销售额（Total sales per week）随时间（周数 Week）推移的波动与变化趋势。
3. **图表布局**：单面板（单panel）设计，采用标准单X轴和单Y轴。
4. **标注方式**：
   * **标题**：无图表标题。
   * **坐标轴**：
     * 横坐标（X轴）标签为“Week”，刻度数值包含0、50、100、150。
     * 纵坐标（Y轴）标签为“Total sales per week”（逆时针旋转90度），刻度数值包含500、1000、1500、2000。
   * **图例**：无图例。
5. **水印内容**：无水印。


## p11_img1.jpg (p.11, 600×247)

根据您的要求，对该学术论文插图的描述如下：

1. **图表类型**：其他（属于统计软件/R语言控制台的文本输出截图，呈结构化表格排版）。
2. **传达的核心信息**：该图展示了对名为 "myts" 的时间序列数据进行 ARIMA(3,0,2) 模型拟合的统计结果，包括模型系数、标准误差、拟合优度指标（如 AIC、BIC）以及训练集的预测误差度量。
3. **图表布局**：采用自上而下的纵向文本排版结构，主要分为四个逻辑板块：
   * 顶部：模型与序列定义；
   * 上部：系数估计值与标准误表格；
   * 中部：模型拟合统计量（方差、对数似然值及信息准则）；
   * 底部：训练集预测误差的多指标表格。
4. **标注方式**：
   * **标题/模型信息**：顶部标注了序列名称 `Series: myts` 及模型类型 `ARIMA(3,0,2) with non-zero mean`。
   * **系数表格标注**：列标签为自回归参数（`ar1`, `ar2`, `ar3`）、移动平均参数（`ma1`, `ma2`）和均值（`mean`），行标签标有标准误（`s.e.`）。
   * **统计量标注**：标有 `sigma^2`、`log likelihood`、`AIC`、`AICc` 和 `BIC` 等模型评估指标名称。
   * **误差指标标注**：列标签为各种误差度量缩写（`ME`, `RMSE`, `MAE`, `MPE`, `MAPE`, `MASE`, `ACF1`），行标签为 `Training set`。
5. **水印内容**：无。


## p12_img0.jpg (p.12, 594×497)

基于您提供的学术论文插图，解析如下：

1. **图表类型**：柱状图（多面板分面柱状图）。
2. **传达的核心信息**：展示了六类不同农产品（花菜类、花叶类、辣椒类、茄类、食用菌、水生根茎类）在一周内不同时间（星期名）的星期销售比例分布及波动趋势。
3. **图表布局**：多panel布局（2×3的网格分面，共6个子图）。
4. **标注方式**：
   * **子图标题**：每个子图顶部标有对应类别，分别为“花菜类”、“花叶类”、“辣椒类”、“茄类”、“食用菌”、“水生根茎类”。
   * **坐标轴**：
     * **y轴（纵轴）**：标签为“星期销售比例”，刻度范围包含0.00、0.05、0.10、0.15。
     * **x轴（横轴）**：标签为“星期名”，刻度标注为2、4、6。
   * **图例**：无图例。
5. **水印内容**：无水印。


## p12_img1.jpg (p.12, 578×429)

针对这张学术论文插图，以下是具体的描述：

1. **图表类型**
   * **折线图**（多线折线图）。

2. **传达的核心信息**
   * 展示了六种蔬菜分类在星期一至星期日（星期名1-7）期间“星期销售比例”的变化趋势，反映出各蔬菜类别的销售比例在周一至周四相对平稳或略有下降，而在周五至周六（4-6）出现大幅激增，并在周末维持在较高水平。

3. **图表布局**
   * **单panel（单面板）布局**，采用单坐标轴系统（单一X轴和单一Y轴）。

4. **标注方式**
   * **标题**：图表无主标题。
   * **坐标轴**：
     * **横坐标（X轴）**：标签为“星期名”，刻度标有数字 2、4、6。
     * **纵坐标（Y轴）**：标签为“星期销售比例”（垂直文本），刻度标有数值 0.12、0.14、0.16、0.18。
   * **图例**：位于图表右侧，图例标题为“分类名称”，包含六个对应不同颜色折线的类别：
     * 花菜类（红橙色折线）
     * 花叶类（黄褐色折线）
     * 辣椒类（绿色折线）
     * 茄类（青色折线）
     * 食用菌（蓝色折线）
     * 水生根茎类（洋红色/粉色折线）

5. **水印内容**
   * **无水印**。


## p12_img2.jpg (p.12, 859×495)

这幅学术论文插图的描述如下：

1. **图表类型**：折线图（多线折线图）。
2. **传达的核心信息**：展示了六种不同蔬菜分类（花菜类、花叶类、辣椒类、茄类、食用菌、水生根茎类）在2021年至2023年期间，“年月销售比例”随时间变化的波动和趋势对比。
3. **图表布局**：单面板、单轴设计（单一的横坐标轴和纵坐标轴）。
4. **标注方式**：
   * **坐标轴**：
     * 横坐标轴（X轴）标签为“年月”，刻度标记为年份（“2021”、“2022”、“2023”）。
     * 纵坐标轴（Y轴）标签为“年月销售比例”，刻度标记为比例数值（如0.02、0.04、0.06）。
   * **图例**：位于图表右侧，标题为“分类名称”，包含六个类别（花菜类、花叶类、辣椒类、茄类、食用菌、水生根茎类），每个类别对应一种特定颜色的折线。
   * **标题**：图表内无主标题。
5. **水印内容**：无水印。


## p13_img0.jpg (p.13, 464×280)

这幅学术论文插图的描述如下：

1. **图表类型**：折线图（Line Chart）。
2. **传达的核心信息**：该图展示了自2022年7月至2023年年中期间，六种不同蔬菜/农产品分类的“年月销售比例”随时间变化的波动趋势。
3. **图表布局**：单Panel、单Y轴布局。
4. **标注方式**：
   * **横轴（X轴）**：标签为“年月”，标注了时间节点（如“7月 2022”、“10月 2022”、“1月 2023”、“4月 2023”）。
   * **纵轴（Y轴）**：标签为“年月销售比例”，标注了比例数值。
   * **图例**：位于图表右侧，标题为“分类名称”，包含六个对应不同颜色折线的类别：花菜类、花叶类、辣椒类、茄类、食用菌、水生根茎类。
   * **标题**：图表上方无显式主标题。
5. **水印内容**：无水印。


## p13_img1.jpg (p.13, 437×396)

这是一张学术论文中常见的插图，其具体描述如下：

1. **图表类型**：
   * 热力图（具体为相关性矩阵热力图）。

2. **传达的核心信息**：
   * 该图展示了“茄类”、“花菜类”、“花叶类”、“水生根茎类”、“辣椒类”和“食用菌”六个品类之间两两对应的相关系数，用以表现它们之间的关联强度和正负相关关系。

3. **图表布局**：
   * **单panel布局**：主要由一个 $6 \times 6$ 的对称方格矩阵构成，右侧配有一个垂直的颜色渐变条（Colorbar）作为图例。

4. **标注方式**：
   * **坐标轴标签**：
     * **横轴（置于图表上方）**：标签为红色字体，垂直向上排列，内容依次为“茄类”、“花菜类”、“花叶类”、“水生根茎类”、“辣椒类”、“食用菌”。
     * **纵轴（置于图表左侧）**：标签为红色字体，水平排列，内容与横轴完全一致。
   * **数值标注**：矩阵中的每一个单元格内都标有具体的相关系数数值（例如对角线上均为1，其余为介于-1到1之间的实数）。
   * **图例标注**：右侧颜色条标有从 -1 到 1 的数值刻度（间隔为0.2），用蓝色表示正相关，橙/红色表示负相关。

5. **水印内容**：
   * 无水印。


## p13_img2.jpg (p.13, 475×286)

这是一幅学术论文插图的描述：

1. **图表类型**：折线图
2. **传达的核心信息**：展示了从2021年7月至2022年中期间，六种不同品类蔬菜（花菜类、花叶类、辣椒类、茄类、食用菌、水生根茎类）的月度销售比例随时间变化的趋势。
3. **图表布局**：单轴布局（单一Y轴与单一X轴）。
4. **标注方式**：
   * **标题**：无图表主标题。
   * **坐标轴**：
     * **横坐标轴（X轴）**：标签为“年月”，标注有时间刻度“7月 2021”、“10月 2021”、“1月 2022”、“4月 2022”。
     * **纵坐标轴（Y轴）**：标签为“年月销售比例”，刻度标注为“0.01”、“0.02”、“0.03”、“0.04”。
   * **图例**：位于图表右侧，标题为“分类名称”，包含六个类别及对应的颜色：
     * 花菜类（红橙色线）
     * 花叶类（黄褐色线）
     * 辣椒类（绿色线）
     * 茄类（青蓝色线）
     * 食用菌（蓝色线）
     * 水生根茎类（洋红色线）
5. **水印内容**：无水印。


## p13_img3.jpg (p.13, 431×396)

基于您提供的学术论文插图，以下是详细的中文描述：

1. **图表类型**
   * **热力图**（具体为相关性矩阵热力图，Correlation Matrix Heatmap）。

2. **传达的核心信息**
   * 该图展示了六种食品/农产品类别（食用菌、水生根茎类、茄类、花叶类、辣椒类、花菜类）两两之间的相关性系数，反映了不同类别之间的正相关、负相关或弱相关关系。

3. **图表布局**
   * **单面板（Single panel）**布局。整体呈 6×6 的对称矩阵网格结构，右侧附带一个垂直的颜色刻度条（Colorbar）。

4. **标注方式**
   * **横轴（位于图表顶部）：** 垂直旋转书写的红色中文字体，标注了六个类别名称（食用菌、水生根茎类、茄类、花叶类、辣椒类、花菜类）。
   * **纵轴（位于图表左侧）：** 水平书写的红色中文字体，标注了与横轴完全对应的六个类别名称。
   * **单元格标注：** 每个网格单元内均标注了具体的相关性系数数值（对角线均为 1）。
   * **图例（位于图表右侧）：** 垂直渐变色条，范围从 -1 到 1。深蓝色表示强正相关（最大值为 1），白色表示不相关（0 附近），橙红色表示强负相关（最小值为 -1）。
   * **标题：** 图表中无独立标题。

5. **水印内容**
   * 无水印。


## p14_img0.jpg (p.14, 491×296)

这是一张学术论文中的插图，其具体描述如下：

1. **图表类型**：折线图。
2. **传达的核心信息**：展示了六种蔬菜品类（花菜类、花叶类、辣椒类、茄类、食用菌、水生根茎类）在2020年7月至2021年中期期间的“年月销售比例”随时间变化的趋势。
3. **图表布局**：单轴布局（包含单个X轴和单个Y轴，无多Panel）。
4. **标注方式**：
   * **标题**：无图表内嵌标题。
   * **坐标轴**：
     * **横坐标（X轴）**：标签为“年月”，时间刻度标注有“7月 2020”、“10月 2020”、“1月 2021”和“4月 2021”。
     * **纵坐标（Y轴）**：标签为“年月销售比例”，数值刻度范围为0.01至0.05。
   * **图例**：位于图表右侧，标题为“分类名称”，包含六个图例项：“花菜类”（红色）、“花叶类”（黄褐色）、“辣椒类”（绿色）、“茄类”（青色）、“食用菌”（蓝色）、“水生根茎类”（粉色）。
5. **水印内容**：无。


## p14_img1.jpg (p.14, 456×411)

基于您提供的学术论文插图，以下是详细的中文描述：

1. **图表类型**
   * **热力图**（具体为**相关性矩阵热力图** / Correlation Matrix Heatmap）。

2. **传达的核心信息**
   * 该图展示了水生根茎类、食用菌、花菜类、花叶类、辣椒类和茄类这六种蔬菜品类之间的两两相关性关系（包括相关系数的正负及强弱）。

3. **图表布局**
   * **单面板布局（Single panel）**。图表主体为一个 6×6 的对称方阵，右侧附带一个垂直分布的渐变色标（Colorbar）作为图例。

4. **标注方式**
   * **坐标轴/标签**：
     * **纵轴（左侧）**：从上到下依次为红色的水平文本标签：“水生根茎类”、“食用菌”、“花菜类”、“花叶类”、“辣椒类”、“茄类”。
     * **横轴（顶部）**：从左到右依次为相同的红色垂直文本标签：“水生根茎类”、“食用菌”、“花菜类”、“花叶类”、“辣椒类”、“茄类”。
   * **数值标注**：热力图的每个单元格内部均用黑色加粗字体标注了具体的数值（相关系数）。
   * **图例**：右侧的色条标注了数值刻度，范围从 -1 到 1。蓝色系代表正相关（最高为 1），红色系代表负相关（最低为 -1），白色代表无相关性（0 附近）。
   * **标题**：图中无独立标题。

5. **水印内容**
   * 无水印。


## p14_img2.jpg (p.14, 542×421)

这是一张学术论文插图的详细描述：

1. **图表类型**：柱状图（分面柱状图/多面板柱状图）。
2. **传达的核心信息**：展示了六个不同食品/蔬菜品类（花菜类、花叶类、辣椒类、茄类、食用菌、水生根茎类）在三个年度区间（20-21年、21-22年、22-23年）内的年销量比例及其变化趋势。
3. **图表布局**：多panel布局。图表采用2行3列的网格设计，共有6个相互独立的子图面板，它们共享相同的横坐标轴和纵坐标轴范围。
4. **标注方式**：
   * **子标题**：每个子面板上方分别标注了品类名称，依次为“花菜类”、“花叶类”、“辣椒类”、“茄类”、“食用菌”和“水生根茎类”。
   * **纵坐标（Y轴）**：位于图表左侧，标签为“年销量比例”，数值刻度范围为0.0至0.5。
   * **横坐标（X轴）**：位于图表底部中央，标签为“年”，数值刻度分别为“20-21”、“21-22”和“22-23”。
   * **图例**：无图例。
5. **水印内容**：无水印。


## p14_img3.jpg (p.14, 528×409)

针对您提供的学术论文插图，以下是具体的描述：

1. **图表类型**：折线图
2. **传达的核心信息**：展示了六种不同蔬菜分类（花菜类、花叶类、辣椒类、茄类、食用菌、水生根茎类）在2020-21年、21-22年和22-23年三个年度区间的“年销量比例”变化趋势。
3. **图表布局**：单面板（Single Panel）单轴布局，包含一个横轴（X轴）和一个纵轴（Y轴）。
4. **标注方式**：
   - **横坐标（X轴）**：标签为“年”，刻度为“20-21”、“21-22”、“22-23”。
   - **纵坐标（Y轴）**：标签为“年销量比例”，刻度包含0.3、0.4等数值。
   - **图例**：位于图表右侧，标题为“分类名称”，包含六个类别及其对应的折线颜色（花菜类、花叶类、辣椒类、茄类、食用菌、水生根茎类）。
5. **水印内容**：无水印。


## p15_img0.jpg (p.15, 580×382)

基于您提供的学术论文插图，以下是详细的描述：

1. **图表类型**：
   * **折线图**（具体为分面多线图/Faceted Line Chart）。

2. **传达的核心信息**：
   * 本图展示了六个不同食品/蔬菜大类（花菜类、花叶类、辣椒类、茄类、食用菌、水生根茎类）在一年中各月份（1月至12月）的月销量比例变化趋势及分布特征，用以对比不同品类的季节性销售规律。

3. **图表布局**：
   * **多panel布局**（分面布局）：图表由6个子图组成，呈3行2列排列。每个子图代表一个特定的分类，所有子图共用相同的横坐标轴（月份）和纵坐标轴（月销量比例）范围。

4. **标注方式**：
   * **标题**：每个子图顶部均有该分类的名称，分别为“花菜类”、“花叶类”、“辣椒类”、“茄类”、“食用菌”和“水生根茎类”。
   * **坐标轴**：
     * **横坐标**：标签为“月份”，刻度显示时间区间。
     * **纵坐标**：标签为“月销量比例”，刻度范围为0.00至1.00。
   * **图例**：位于整幅图的右侧，标题为“分类名称”，通过六种不同的线条颜色（红色、黄褐色、绿色、青色、蓝色、粉红色）对应区分上述六个类别。

5. **水印内容**：
   * 无水印。


## p15_img1.jpg (p.15, 579×387)

基于您提供的学术论文插图，以下是详细的描述：

**1. 图表类型**
*   **折线图**（具体为多面板/分面折线图）。

**2. 传达的核心信息**
*   该图展示了六类蔬菜（花菜类、花叶类、辣椒类、茄类、食用菌、水生根茎类）在2020-21、2021-22和2022-23三个年度区间的年销量比例变化趋势。

**3. 图表布局**
*   **多面板（Multi-panel）布局**：图表采用3行2列的网格结构，共包含6个独立的子图。所有子图共享相同的X轴（年份）和Y轴（年销量比例）刻度。

**4. 标注方式**
*   **标题**：每个子图顶部均有对应的分类标题（依次为：花菜类、花叶类、辣椒类、茄类、食用菌、水生根茎类）。
*   **坐标轴**：
    *   **Y轴**：位于图表左侧，标签为“年销量比例”，刻度区间为0.00到1.00。
    *   **X轴**：位于图表底部，标签为“年份”，刻度值为“20-21”、“21-22”和“22-23”。
*   **图例**：位于图表右侧，标题为“分类名称”，用六种不同的颜色区分六个类别（红色代表花菜类、黄褐色代表花叶类、绿色代表辣椒类、青色代表茄类、蓝色代表食用菌、粉色代表水生根茎类）。

**5. 水印内容**
*   无水印。


## p15_img2.jpg (p.15, 932×820)

针对您提供的学术论文插图，解析如下：

1. **图表类型**
   * **热力图**（结合了层次聚类树状图/Clustered Heatmap）。

2. **传达的核心信息**
   * 该图展示了多种蔬菜品种（如奶白菜、螺丝椒、金针菇等）之间的相关性（或相似度）矩阵，并通过树状图对这些蔬菜进行了层次聚类分类。

3. **图表布局**
   * **单panel布局**。中心为正方形的热力图矩阵，左侧和上侧分别对称地附带了用于展示聚类关系的层次树状图（Dendrogram）。

4. **标注方式**
   * **标题**：无。
   * **坐标轴/标签**：
     * **右侧（纵轴）**：标有各蔬菜品类的中文名称（如“金针菇(盒)”、“螺丝椒(份)”、“奶白菜”等）。
     * **下方（横轴）**：同样对应标有旋转了90度的蔬菜品类中文名称，与右侧标签形成矩阵对应。
     * **左侧与上侧**：无文字坐标轴，仅有表示聚类结构的树状分支线。
   * **图例**：图中未显示颜色数值对照的图例（通常红蓝交织代表正负相关性或高低相似度，但本图中未直接呈现Legend）。

5. **水印内容**
   * 无水印。


## p15_img3.jpg (p.15, 505×45)

基于您提供的学术论文插图，以下是详细描述：

1. **图表类型**：其他（数学公式图片）。
2. **传达的核心信息**：该公式展示了变量 $w_{rep}$ 的计算方法，即通过基础值 $p$ 加上系数 $\alpha$ 与差值 $(w_{mean} - p)$ 的乘积来确定。
3. **图表布局**：不适用（该图为单行数学公式，无坐标轴或多面板布局）。
4. **标注方式**：无标题、坐标轴或图例。图中仅包含数学公式本身：$w_{rep} = p + \alpha * (w_{mean} - p)$。
5. **水印内容**：无水印。


## p19_img0.jpg (p.19, 989×590)

这幅学术论文插图的描述如下：

1. **图表类型**：折线图与散点图结合的时间序列预测图（这是典型的 Facebook Prophet 模型预测输出图）。
2. **传达的核心信息**：展示了目标变量 $y$ 随时间 $ds$ 的历史观测数据点、模型的拟合/预测趋势线以及预测的置信区间范围（约从2020年中旬至2023年中旬）。
3. **图表布局**：单轴（单面板二维坐标系，配有浅灰色网格背景）。
4. **标注方式**：
   * **坐标轴**：横坐标轴标签为 “ds”（代表时间，刻度格式为年-月，如 2020-09, 2021-01 等）；纵坐标轴标签为 “y”（代表数值）。
   * **图例**：无显式图例。但通过视觉元素进行隐式标注——黑色实心圆点表示实际观测值（y），深蓝色折线表示模型预测值，浅蓝色阴影区域表示预测的置信区间。
   * **标题**：无图表标题。
5. **水印内容**：无水印。


## p20_img0.jpg (p.20, 888×1489)

基于您提供的学术论文插图，以下是详细的描述：

1. **图表类型**
   * **折线图**（具体为多子图时间序列分解折线图）。

2. **传达的核心信息**
   * 该图展示了时间序列预测模型（如 Prophet 模型）的预测结果分解，分别呈现了趋势（trend）、节日效应（holidays）、周季节性（weekly）、年季节性（yearly）以及附加回归量效应（extra_regressors_additive）对预测目标的贡献和变化规律。

3. **图表布局**
   * **多panel布局**：由垂直堆叠的5个子图（Panel）组成，每个子图独立展示一个分量的变化曲线。

4. **标注方式**
   * **坐标轴**：
     * **Y轴**：每个子图拥有独立的Y轴，标签分别对应其代表的分量：`trend`（趋势）、`holidays`（节日）、`weekly`（周效应）、`yearly`（年效应）和`extra_regressors_additive`（附加回归量）。
     * **X轴**：
       * 第1、2、5个子图的X轴为日期时间（标注为 `ds`），以“年-月”格式呈现（如 2020-09, 2021-01 等）。
       * 第3个子图的X轴为星期几（标注为 `Day of week`），范围从 Sunday 到 Saturday。
       * 第4个子图的X轴为一年中的日期（标注为 `Day of year`），以具体的月份第一天呈现（如 January 1, March 1 等）。
   * **图例与标题**：无全局标题，无子图标题，无图例。所有子图均配有灰色网格背景以便于读取趋势。

5. **水印内容**
   * 无水印。


## p22_img0.jpg (p.22, 546×432)

针对这张学术论文插图，解析如下：

1. **图表类型**：散点图结合线性拟合线（Scatter Plot with Fit Line）。
2. **传达的核心信息**：展示了“花叶类”样本数据的分布特征，并表明其两个变量之间存在显著的负相关关系及线性拟合趋势。
3. **图表布局**：单面板单轴布局（Single-panel, Single-axis layout），即由一个主图区域和单套直角坐标轴构成。
4. **标注方式**：
   * **标题**：图表正上方有中文标题“花叶类”。
   * **坐标轴**：X轴和Y轴均有数值刻度（X轴范围约为2至11，Y轴范围约为50至280），但均未标注坐标轴物理量的名称或单位。
   * **图例**：位于图表右上角，用蓝色圆点标注“Data Points”（数据点），红色实线标注“Fit Line”（拟合线）。
   * **文字标注**：图表左上角标有拟合参数的具体数值：
     * 斜率（Slope）：-23.68
     * 截距（Intercept）：315.37
     * 相关系数（Correlation Coefficient）：-0.81
5. **水印内容**：无水印。


## p22_img1.jpg (p.22, 539×433)

这是一张学术论文插图的描述：

1. **图表类型**：散点图（包含线性拟合线）。
2. **传达的核心信息**：展示了“花菜类”数据中两个变量之间呈较强的负线性相关关系，即随着横坐标数值的增加，纵坐标数值呈现明显的下降趋势（相关系数为 -0.85）。
3. **图表布局**：单面板（单轴直角坐标系）。
4. **标注方式**：
   * **标题**：顶部正中设有中文标题“花菜类”。
   * **坐标轴**：横轴和纵轴均有数值刻度，但未标注坐标轴名称（无轴标签）。
   * **图例**：右上角设有图例，其中蓝色圆点标注为“Data Points”（数据点），红色实线标注为“Fit Line”（拟合线）。
   * **文本标注**：左上角标注了拟合线的具体参数，包括斜率（Slope: -3.46）、截距（Intercept: 71.03）和相关系数（Correlation Coefficient: -0.85）。
5. **水印内容**：无水印。


## p22_img2.jpg (p.22, 546×433)

这是一张学术论文插图的描述：

1. **图表类型**：散点图（带有线性拟合线）。
2. **传达的核心信息**：该图展示了“辣椒类”相关数据变量之间呈现负相关关系，拟合线的斜率为-4.54，截距为123.86，相关系数为-0.66。
3. **图表布局**：单panel，单轴（标准单一X轴和Y轴布局）。
4. **标注方式**：
   * **标题**：顶部正中标题为“辣椒类”。
   * **坐标轴**：X轴和Y轴仅有数值刻度，无坐标轴名称/单位标注。
   * **图例**：右上角设有图例，蓝色圆点代表“Data Points”（数据点），红色实线代表“Fit Line”（拟合线）。
   * **文本标注**：左上角标有拟合公式参数，包含 “Slope: -4.54”（斜率）、“Intercept: 123.86”（截距）和 “Correlation Coefficient: -0.66”（相关系数）。
5. **水印内容**：无水印。


## p22_img3.jpg (p.22, 546×432)

这是一幅学术论文插图的描述：

1. **图表类型**：散点图与线性拟合线图（Scatter plot with a fit line）。
2. **传达的核心信息**：该图展示了与“食用菌”相关的两个变量之间呈现中等强度的负线性相关关系（相关系数为-0.55）。
3. **图表布局**：单panel布局，单轴系统（横轴和纵轴各一个）。
4. **标注方式**：
   * **标题**：图表正上方有中文标题“食用菌”。
   * **坐标轴**：X轴和Y轴均仅有数值刻度，未标注具体的坐标轴名称或单位。
   * **图例**：位于图表右上方，蓝色圆点代表“Data Points”（数据点），红色实线代表“Fit Line”（拟合线）。
   * **其他文本标注**：左上方标有拟合分析的具体参数，包括斜率（Slope: -4.49）、截距（Intercept: 108.94）和相关系数（Correlation Coefficient: -0.55）。
5. **水印内容**：无水印。


## p22_img4.jpg (p.22, 539×433)

这是一幅学术论文插图的描述：

1. **图表类型**：散点图（带有线性拟合线）。
2. **传达的核心信息**：展示了“水生根茎类”数据点的分布规律及其呈现负相关的线性拟合趋势（拟合斜率为-0.77，相关系数为-0.33）。
3. **图表布局**：单面板（单个二维直角坐标系，包含一个X轴和一个Y轴）。
4. **标注方式**：
   * **标题**：顶部中央设有中文标题“水生根茎类”。
   * **坐标轴**：X轴和Y轴均有数值刻度，但未标注具体的坐标轴名称或单位。
   * **图例**：右上角设有图例，其中蓝色圆点表示“Data Points”（数据点），红色实线表示“Fit Line”（拟合线）。
   * **文本标注**：左上角印有拟合统计参数，具体内容为：
     * `Slope: -0.77`（斜率）
     * `Intercept: 44.89`（截距）
     * `Correlation Coefficient: -0.33`（相关系数）
5. **水印内容**：无。


## p22_img5.jpg (p.22, 539×433)

这幅学术论文插图的描述如下：

1. **图表类型**：散点图（附带线性回归拟合线）。
2. **传达的核心信息**：展示了“茄类”相关数据的分布状态，并指出两者之间存在微弱的负线性相关关系（斜率为-0.33，相关系数为-0.21）。
3. **图表布局**：单面板（Single panel），采用标准的二维笛卡尔坐标系。
4. **标注方式**：
   * **标题**：图表正上方标有中文标题“茄类”。
   * **坐标轴**：X轴和Y轴均有数值刻度，但未标注具体的坐标轴名称或单位。
   * **图例**：位于图表右上角，用蓝色圆点标注“Data Points”（数据点），用红色实线标注“Fit Line”（拟合线）。
   * **文本标注**：图表左上角标有拟合统计信息，具体为：
     * `Slope: -0.33`（斜率）
     * `Intercept: 24.13`（截距）
     * `Correlation Coefficient: -0.21`（相关系数）
5. **水印内容**：无水印。


## p23_img0.jpg (p.23, 564×450)

基于您提供的学术论文插图，以下是详细的描述：

1. **图表类型**：
   * 散点图与线性拟合图（折线图）。

2. **传达的核心信息**：
   * 该图展示了“花叶类”样本中，原始数据（Original Data）与中间值（Mid Values）随变量X的增加而呈现出明显的负相关（下降趋势），并提供了一条红色的线性拟合线（Fitted Line）来表征其总体趋势。

3. **图表布局**：
   * 单面板布局（Single panel），采用标准的二维单轴系统（即单个X轴与单个Y轴）。

4. **标注方式**：
   * **标题**：图表正上方设有中文标题“**花叶类**”。
   * **坐标轴**：横坐标轴标注为“**X**”，纵坐标轴标注为“**Y**”。
   * **图例**：位于图表右上角，包含三项：
     * 灰色圆点：**Original Data**（原始数据）
     * 蓝色圆点：**Mid Values**（中间值）
     * 红色实线：**Fitted Line**（拟合线）

5. **水印内容**：
   * 无水印。


## p23_img1.jpg (p.23, 557×450)

这是一张学术论文插图的描述：

1. **图表类型**：散点图（带有线性拟合曲线）。
2. **传达的核心信息**：展示了“花菜类”数据中变量X与Y之间的负相关关系，并对比了原始数据（Original Data）、中间值（Mid Values）以及对应的拟合直线（Fitted Line）的分布和趋势。
3. **图表布局**：单面板（Single Panel）布局，使用单组X-Y直角坐标轴。
4. **标注方式**：
   * **标题**：图表正上方有中文标题“花菜类”。
   * **坐标轴**：横坐标轴标签为“X”，纵坐标轴标签为“Y”。
   * **图例**：位于图表右上角，包含：
     * 灰色半透明圆点：代表“Original Data”（原始数据）
     * 蓝色圆点：代表“Mid Values”（中间值）
     * 红色实线：代表“Fitted Line”（拟合线）
5. **水印内容**：无水印。


## p23_img2.jpg (p.23, 564×450)

这是一张学术论文插图的详细描述：

1. **图表类型**：散点图与拟合曲线图（Scatter Plot with Fitted Line）。
2. **传达的核心信息**：展示了“辣椒类”数据中变量X与变量Y之间的负相关关系，通过原始数据、中间值数据以及一条红色的线性拟合线呈现了随X增加Y整体呈下降趋势的规律。
3. **图表布局**：单面板（单轴）布局。
4. **标注方式**：
   * **标题**：图表上方中央有中文标题“辣椒类”。
   * **坐标轴**：横坐标标签为“X”，纵坐标标签为“Y”。
   * **图例**：位于图表右上角，包含三项：
     * 灰色半透明圆点表示“Original Data”（原始数据）
     * 蓝色圆点表示“Mid Values”（中间值）
     * 红色实线表示“Fitted Line”（拟合线）
5. **水印内容**：无水印。


## p23_img3.jpg (p.23, 564×450)

基于您提供的学术论文插图，以下是详细的中文描述：

1. **图表类型**
   * **散点图与拟合线图**（包含散点与一条线性拟合折线）。

2. **传达的核心信息**
   * 该图展示了关于“食用菌”的数据中变量 X 与 Y 之间的负相关关系，通过呈现原始数据点、筛选出的中间值点以及最终的一元线性拟合红线，反映了 Y 随 X 增加而呈下降的趋势。

3. **图表布局**
   * **单轴（单面板）布局**：包含单一的二维坐标系（一个横轴和一个纵轴）。

4. **标注方式**
   * **标题**：图表正上方设有中文标题“**食用菌**”。
   * **坐标轴**：横坐标轴标签为“**X**”，纵坐标轴标签为“**Y**”。
   * **图例**：位于图表右上角，包含三项说明：
     * **Original Data**（灰色半透明圆点）：代表原始数据。
     * **Mid Values**（蓝色实心圆点）：代表中间值。
     * **Fitted Line**（红色实线）：代表拟合线。

5. **水印内容**
   * 无水印。


---
*40/40 figures described*