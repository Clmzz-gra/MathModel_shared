# 模型三：跨疾病预测模型

## 一、模型构建

跨疾病预测要回答的问题是，在一种疾病上学到的微生物特征能否迁移到另一种从未见过的疾病上。这比单疾病预测更接近真实应用场景——新疾病出现时往往没有标注数据。我们采用留一疾病（Leave-One-Disease-Out, LODO）方案作为诚实评估框架：每次留出一种疾病作测试集，其余两种作训练集，保证测试疾病在训练阶段完全不可见。在此框架下系统对比四种跨疾病建模策略，结果四策略三组合 AUC 均值全部低于 0.60，触发预设的后续模型探索流程；候选模型序列中最优的密度比重加权域适应均值为 0.6068，仍低于本文设定的可用性参考线（后简称可用线）0.65。因此本节以"最优可达性能加完整验证过程"的方式交付，未声称得到可用的跨疾病模型。

### 1.1 LODO 留一疾病评估方案

LODO 的核心思想是把疾病本身作为划分维度，而非随机划分样本。设三种疾病构成集合 $\mathcal{D}=\{d_1,d_2,d_3\}$（$d_1$=CRC、$d_2$=IBD、$d_3$=Obesity）。对每个被留出的疾病 $d_k$，构造训练–测试划分：
$$
\mathcal{T}_k = \bigcup_{d\neq d_k} \mathcal{S}_d, \qquad \mathcal{E}_k = \mathcal{S}_{d_k}, \qquad k=1,2,3
$$
其中 $\mathcal{S}_d$ 是疾病 $d$ 的样本集，$\mathcal{T}_k$ 与 $\mathcal{E}_k$ 分别为第 $k$ 组合的训练集与测试集。

三个组合分别为：C1 以 IBD 和 Obesity 为训练集、CRC 为测试集；C2 以 CRC 和 Obesity 为训练集、IBD 为测试集；C3 以 CRC 和 IBD 为训练集、Obesity 为测试集。三个测试集的正类占比差异较大——C1 为 39.7%，C2 仅 22.7%（少数类），C3 高达 64.8%（正类多数），这种基线差异本身就是跨疾病应用的现实挑战。

| 组合 | 训练集（2 疾病） | 测试集（1 疾病） | 测试集正类占比 |
|---|---|---|---|
| C1 | IBD + Obesity | CRC | 39.7% |
| C2 | CRC + Obesity | IBD | 22.7%（少数类） |
| C3 | CRC + IBD | Obesity | 64.8%（正类多数） |

### 1.2 数据预处理与 CLR 变换

物种级相对丰度本质上是成分数据，各物种丰度之和为 1，存在定和约束。如果直接将原始丰度输入基于欧式距离的模型，定和约束会引入伪相关——一个物种丰度的上升必然意味着其他物种的下降，这种数学约束会被误判为生物学关联。因此采用中心对数比变换（CLR）将成分数据映射到无约束的实数空间：
$$
\mathrm{clr}(\mathbf{x})_j = \ln\!\left(\frac{x_j}{g(\mathbf{x})}\right), \qquad g(\mathbf{x}) = \left(\prod_{j=1}^{p} x_j\right)^{1/p}
$$
其中 $g(\mathbf{x})$ 是几何均值。零值（微生物未检出）不能直接取对数，采用乘法替换：
$$
x_{ij} \leftarrow \delta = 6.5\times10^{-6}, \qquad \text{当 } x_{ij}=0
$$

预处理分三步：首先过滤近全零特征，将 1331 个原始特征压缩至 264 个；然后对零值做乘法替换后执行 CLR 变换；最后对每个特征做标准化，但均值和标准差仅在训练集上估计，再应用到测试集：
$$
z_{ij} = \frac{\mathrm{clr}(\mathbf{x}_i)_j - \hat{\mu}_j^{(\mathcal{T}_k)}}{\hat{\sigma}_j^{(\mathcal{T}_k)}}
$$
这一步严格避免任何形式的数据泄漏。

### 1.3 策略 A：直接迁移（基线）

策略 A 是最直接的跨疾病做法：在两种疾病的合并训练集上拟合 L2 正则化 Logistic 回归，直接在第三种疾病上测试。它作为所有策略的性能基线，沿用 S1 的模型设置（L2+CLR，物种级 264 特征）。

对每个 LODO 组合 $k$，在训练集 $\mathcal{T}_k$ 上拟合正则化 Logistic 回归：
$$
P(y=1 \mid \mathbf{x}) = \sigma\!\left(\mathbf{w}^{\top}\mathbf{z} + b\right) = \frac{1}{1+\exp\!\left(-\left(\mathbf{w}^{\top}\mathbf{z}+b\right)\right)}
$$
其中 $\mathbf{z}=\mathrm{clr}(\mathbf{x})$ 标准化后的特征向量，原始分数（logit）记作 $f(\mathbf{x})=\mathbf{w}^{\top}\mathbf{z}+b$。优化目标为 L2 正则化加类别权重：
$$
\min_{\mathbf{w},b}\ \frac{1}{2}\|\mathbf{w}\|_2^2 + C \sum_{i\in\mathcal{T}_k} w_i \left[ -y_i\ln\sigma(f(\mathbf{x}_i)) - (1-y_i)\ln\!\left(1-\sigma(f(\mathbf{x}_i))\right) \right]
$$
其中 $C=1.0$ 是正则化强度，$w_i$ 是类别权重（`class_weight='balanced'`），用于处理测试集类别不平衡。

### 1.4 策略 B：共享标志物（跨疾病通用模型）

策略 B 的出发点是：如果疾病特异信号在跨疾病场景下反而成为噪声，那么只保留三种疾病共享的物种特征，或许能训练出更通用的模型。共享特征集取三种疾病特征集的交集：
$$
\mathcal{F}_{\mathrm{shared}} = \mathcal{F}_{\mathrm{CRC}} \cap \mathcal{F}_{\mathrm{IBD}} \cap \mathcal{F}_{\mathrm{Obesity}}
$$
策略 B 在 $\mathcal{F}_{\mathrm{shared}}$ 上重训策略 A 同款模型，重跑 LODO。需要强调的是，这里的交集仅基于特征的存在性，绝不涉及测试集标签，属于转导式边界内的合法操作。

### 1.5 策略 C：分类学聚合（属级/门级 + CLR）

题面注 2 明确提示可利用微生物分类学信息。策略 C 将物种级丰度按属或门聚合，期望更高层级的分类单元具有更强的跨疾病保守性。对某个属 $g$，其聚合丰度为该属下所有物种丰度之和：
$$
x_g = \sum_{j:\ \text{species } j \in \text{genus } g} x_j
$$
聚合后维度大幅下降（属级 106 维、门级 11 维），再对聚合后的成分做 CLR，重训 Logistic L2，重跑 LODO。

### 1.6 策略 D：Platt 校准（输出概率修正模型）

AUC 是阈值无关指标，不受概率调整影响。但模型实际应用时需要输出可解释的患病概率，而训练集和测试集的患病基线不同会导致概率失准。策略 D 在 AUC 最优的基策略上叠加 Platt 校准，将原始 logit 分数映射到调整概率：
$$
P_{\mathrm{cal}}(y=1 \mid \mathbf{x}) = \frac{1}{1+\exp\!\left(A\cdot f(\mathbf{x}) + B\right)}
$$
其中 $A,B$ 是标量参数，仅在训练集上用 Logistic 回归拟合。当 $A<0$ 时 $P_{\mathrm{cal}}$ 是 $f$ 的严格单调递增函数，不改变样本排序，故 AUC 完全不变——Platt 校准修复的是分数到概率的映射，不改排序能力。

### 1.7 阈值迁移：Youden J

跨疾病场景下，决策阈值不能在测试集上重新确定——那会用到新疾病的标签，破坏测试集信息隔离。采用 Youden J 统计量在训练集上确定最优阈值，然后原样迁移到测试集。对决策阈值 $\tau$，Youden J 为灵敏度与特异度之和减 1：
$$
J(\tau) = \mathrm{TPR}(\tau) + \mathrm{TNR}(\tau) - 1 = \mathrm{TPR}(\tau) - \mathrm{FPR}(\tau)
$$
Youden J 最优阈值为使 $J$ 最大的阈值：
$$
\tau^{*} = \arg\max_{\tau} J(\tau)
$$
$\tau^{*}$ 只在训练集 $\mathcal{T}_k$ 上估计，然后原样搬到测试集 $\mathcal{E}_k$，报告该阈值下的 ACC、灵敏度、特异度、F1。

### 1.8 后续模型探索流程（R1–R4）

预设候选模型探索触发条件：如果四种策略的三组合 AUC 均值全部低于 0.60，则启动后续模型探索。设策略 $s\in\{\text{A,B,C,D}\}$ 的 3 组合 AUC 均值为 $\overline{\mathrm{AUC}}_s$。触发条件：
$$
\overline{\mathrm{AUC}}_s < 0.60, \qquad \forall s\in\{\text{A,B,C,D}\}
$$
候选模型模型被判定为"可用"需满足以下任一条件：
$$
\overline{\mathrm{AUC}}_r \ge 0.65 \quad \text{或} \quad \overline{\mathrm{AUC}}_r - \overline{\mathrm{AUC}}_{\mathrm{A}} \ge 0.10
$$
候选模型序列包含四个层级：R1 树模型（RandomForest）、R2 样本合并（等价于策略 A）、R3 密度比重加权域适应、R4 对抗式域适应（DANN）。其中 R3 的密度比权重为测试特征分布密度与训练特征分布密度之比：
$$
w(\mathbf{x}) = \frac{p_{\mathrm{test}}(\mathbf{x})}{p_{\mathrm{train}}(\mathbf{x})}
$$
估计密度比时仅使用测试集特征、不使用标签，符合转导式边界。

---

## 二、参数与求解

模型采用 L2 正则化 Logistic 回归，正则化强度 $C=1.0$，使用 balanced 类别权重处理类别不平衡，最大迭代次数 2000。预处理流程为近全零过滤到 CLR 变换到标准化，其中标准化参数仅从训练集估计。预算不受限。Platt 校准的三组合 $A$ 参数均为负，保证单调变换不反转排序。整个建模过程中，测试集的信息不以任何方式进入训练过程：特征选择、CLR、标准化、模型权重、决策阈值、调整参数均仅从训练集获得，阈值迁移不在测试集上重定。

| 参数 | 取值 / 说明 |
|---|---|
| 模型 | LogisticRegression（L2 惩罚，$C=1.0$，`class_weight='balanced'`，`max_iter=2000`）+ CLR |
| 预处理 | 近全零过滤（1331→264）→ CLR（零值乘法替换 $\delta=6.5\times10^{-6}$）→ 标准化（均值/方差仅训练集估计） |
| 预算受限 | `budget_limited`=False |
| Platt 校准 | $A<0$（三组合 $A$ 均负，等价 `platt_w`>0，防排序反转） |
| 信息隔离 | 整个建模过程中测试集的信息不以任何方式进入训练过程；所有参数仅从训练集估计；阈值迁移不在测试集上重定 |

求解流程为：对每个 LODO 组合，在训练集上拟合模型并确定 Youden 最优阈值，在测试集上预测并报告 AUC（主指标）以及阈值迁移后的 ACC、灵敏度、特异度、F1（辅指标）。策略 D 在 AUC 最优的基策略上叠加 Platt 校准后重跑相同流程。

---

## 三、结果

### 3.1 四策略对比

四种策略的三组合 AUC 如下表所示。策略 A 直接迁移的均值最高，为 0.5603，但仍远低于可用线 0.65。策略 B 共享标志物的均值为 0.5572，与策略 A 基本持平，说明剔除疾病特异特征并未带来增益——共享特征本身也不承载可迁移信号。策略 C 分类学聚合反而显著下降，属级聚合均值仅 0.4639，门级聚合 0.5134，表明更高分类层级的保守性是以丢失判别信息为代价的，题面提示的分类学信息在本数据中实证无增益。策略 D 与策略 A 的 AUC 完全相同（0.5603），符合 Platt 单调变换不改变排序的理论预期。

| 策略 | C1(CRC) | C2(IBD) | C3(Obesity) | 3 组合均值 |
|---|---|---|---|---|
| A 直接迁移（L2+CLR 物种级 264） | 0.5674 | 0.5882 | 0.5253 | **0.5603** |
| B 共享标志物（252 特征交集重训） | 0.5417 | 0.6080 | 0.5218 | 0.5572 |
| C 属级聚合（`g__` 求和 106 特征） | 0.3616 | 0.4861 | 0.5440 | 0.4639 |
| C 门级聚合（`p__` 求和 11 特征） | 0.4141 | 0.5261 | 0.5999 | 0.5134 |
| D 输出概率修正（Platt 校准，base=A） | 0.5674 | 0.5882 | 0.5253 | 0.5603 |

![四策略跨疾病 3 组合 AUC 对比](figures/S3-strategy-compare.pdf)

四策略 3 组合 AUC 均值全部低于 0.60（最优策略 A 直接迁移 0.5603），触发后续模型探索。

### 3.2 后续模型探索结果（R1–R4）

候选模型序列逐级测试结果如下表。R1 随机森林均值 0.5092，甚至低于线性模型，说明非线性模型在小样本高维场景下更容易过拟合训练分布。R2 样本合并等价于策略 A，均值 0.5603。R3 密度比重加权域适应均值 0.6068，是候选模型序列中最优的，相对策略 A 提升 +0.0465，但该提升未达到大于等于 0.10 的可用线，且均值本身低于 0.65。R4 对抗式域适应均值 0.5947，略低于 R3。

| 层级 | 模型族 | 3 组合均值 | C1 | C2 | C3 | 达可用线？ |
|---|---|---|---|---|---|---|
| R1 | 树模型（RandomForest 500 树） | 0.5092 | 0.4405 | 0.5529 | 0.5342 | 否 |
| R2 | 样本合并（Logistic，等价策略 A） | 0.5603 | 0.5674 | 0.5882 | 0.5253 | 否 |
| R3 | 密度比重加权（importance weighting） | **0.6068** | 0.5945 | 0.6489 | 0.5771 | 否 |
| R4 | 对抗式域适应（DANN） | 0.5947 | 0.6184 | 0.6259 | 0.5398 | 否 |

R1 到 R4 全部未达可用线。我们穷尽了预设的候选出口，如实报告负结果：在当前数据规模和疾病组合下，跨疾病预测的性能上限约为 0.61（R3），未达到可直接应用的水平。失败的主要原因有二：一是各疾病的判别信号高度特异，共享方向接近随机；二是不同研究中的患病定义存在差异——不同研究中"患病"与"健康"的定义和基线不同，直接迁移会导致决策边界错位。

### 3.3 衰减归因（三分法）

为了量化跨疾病性能衰减的来源，将每种疾病的跨疾病 AUC 与其域内 AUC（同标准 264 特征 5 折 CV）对比，计算衰减量。对疾病 $d$，跨疾病衰减量为：
$$
\Delta_d = \mathrm{AUC}_{\mathrm{cross}}(d) - \mathrm{AUC}_{\mathrm{domain}}(d)
$$
其中 $\mathrm{AUC}_{\mathrm{cross}}(d)$ 是疾病 $d$ 作测试集时的跨疾病 AUC，$\mathrm{AUC}_{\mathrm{domain}}(d)$ 是疾病 $d$ 的域内 AUC。

结果显示 IBD 的衰减最大，为负 0.2706，CRC 次之（负 0.2138），Obesity 最小（负 0.1384）。从归因来看，CRC 和 IBD 的衰减主要由疾病特异信号主导——这两种疾病的域内 AUC 较高（0.78 和 0.86），说明微生物组与疾病状态有强关联，但这种关联是疾病特异的，无法迁移。Obesity 的域内 AUC 本身较低（0.66），衰减主要来自患病定义差异——肥胖的定义在不同研究中可能基于不同的 BMI 阈值，且肠道菌群与肥胖的关联本身较弱。批次效应并非主导因素，silhouette 系数仅 0.070，接近 0，说明三种疾病的样本在特征空间中并未按研究来源明显聚类。

| 疾病 | 域内 AUC | 跨疾病 AUC | 衰减量 | 主导归因 |
|---|---|---|---|---|
| CRC | 0.7811 | 0.5674 | −0.2138 | 疾病特异信号 |
| IBD | 0.8588 | 0.5882 | **−0.2706** | 疾病特异信号（最强） |
| Obesity | 0.6638 | 0.5253 | −0.1384 | 患病定义差异 |

![域内 vs 跨疾病 AUC 衰减归因](figures/S3-decay-attribution.pdf)

### 3.4 深度迁移分析

进一步检验共享物种在不同疾病间的效应方向一致性。在 756 个有效共享物种中，方向一致的有 387 个（51.2%），方向翻转的有 369 个（48.8%），几乎各占一半。符号检验 $p=0.5364$，远不显著。这意味着共享物种的效应方向与随机猜测无异——共享物种的存在性本身不承载可迁移的判别信号，真正的判别信息藏在疾病特异的特征方向中，而这些方向在跨疾病场景下不可用。

![共享标志物跨疾病迁移方向一致性（方向一致 vs 方向翻转）](figures/S3-migration-direction.pdf)

### 3.5 阈值漂移量化（C3）

C3 组合（训练 CRC+IBD，测试 Obesity）的阈值漂移最为严重。训练集与测试集的患病概率基线差：
$$
\Delta_{\mathrm{baseline}} = \hat{P}_{\mathrm{test}}(y=1) - \hat{P}_{\mathrm{train}}(y=1)
$$
训练集患病基线为 0.3160，测试集为 0.6482，基线差 $\Delta=+0.3322$——测试集中近三分之二为正类，而训练集仅约三分之一。在训练集上确定的 Youden 最优阈值 $\tau^{*}=0.9205$，落到测试分数分布的 96.0% 分位，意味着只有 4% 的测试样本被判为患病，灵敏度仅 0.0244，几乎全部误判为健康。

Platt 校准是单调变换，不改变决策边界的相对位置，因此无法修复这种由基线偏移导致的阈值漂移——调整前后 Youden 阈值迁移的灵敏度一致。即便放弃训练集阈值、改用 0.5 的朴素概率阈值，灵敏度也仅 0.1646。C3 的案例清楚表明，跨疾病应用的核心障碍不仅是 AUC 不足，更是训练集拟合的决策阈值在新疾病基线下完全失效。

![C3 阈值漂移诊断（训练/测试患病概率基线 + 决策边界位置）](figures/S3-threshold-drift.pdf)

### 3.6 跨疾病泛化受限的因果分析（E0–E6 实验证据链）

<!-- 队友写作：本节为负结果的因果闭环——用六轮归因实验（E0 批次校正、E1 模型容量、E2 贝叶斯模型族、E3 few-shot 方案敏感性、E5 源分离+融合、E6 深度学习直接分类）排除六类替代解释，证明「统一跨疾病模型不可行」是数据本身限制而非模型选择问题。数字与判读已预填，可润色措辞，勿改表格数字。 -->

为排除「统一跨疾病模型不可行」的替代解释（批次伪影、模型容量不足、模型族限制、评估方案过严、模型组合方式、深度学习表示能力），在 LODO 评估框架下依次完成六轮归因实验，全部复用正式实现同标准（CLR δ=6.5×10⁻⁶、L2 正则 + 按类别样本量反比设定样本权重、标准化仅训练集估计）：

| 实验 | 验证的替代解释 | 做法 | 关键结果 | 结论 |
|---|---|---|---|---|
| E0 ComBat 批次校正 | 三数据集=三批次完全共线，失败是批次伪影？ | ComBat 批次校正（设计变量=疾病标签）后重跑策略 A | 3 组合均值 0.5603 → **0.4960**（Δ=−0.0643），C3 跌至 0.3746 | 证伪：校正不升反降，批次效应不是失败主因 |
| E1 免训练/低容量基线 | 失败是模型容量不足（欠拟合）？ | 免训练 NCM（2 类均值原型）+ 低容量 PLS-DA 重跑 LODO | NCM 0.5362/0.5594、PLS-DA 0.5574/0.5798，全部 ≤ 基线 0.5603 | 证伪：容量从免训练到高容量域适应仅 +0.06，天花板与容量无关 |
| E2 贝叶斯模型族 | 稀疏先验/贝叶斯框架可突破？ | 贝叶斯 L2 / ARD（ADVI），S1 域内 + S3 LODO 双跑 | S1 域内 ARD 不提升（Δ≤+0.003）；S3 贝叶斯 0.5746/0.5748 ≈ 基线 0.5603 | 证伪：S1 的 L2 已近上限，S3 天花板与模型族无关 |
| E3 few-shot 敏感性 | LODO 零样本方案过严，少量标注即可救回？ | 目标疾病 k=50 带标签样本并入训练 | 均值 0.5603 → 0.5898（+0.0295，仍 <0.60）；C1 达 0.6567 | 证伪：零样本方案不过严，信号稀缺为真 |
| E5 源分离+融合 | 合并训练稀释源信号，分别学习再融合可突破？ | 两训练疾病分别训 Logistic（各自独立标准化），F1 logit 平均 / F2 概率平均 / F3 域内 CV-AUC 加权 | 均值 F1 0.5996 / F2 0.5828 / F3 0.5995 vs 基线 0.5603（+0.039）；C1 融合 0.6273、C2 F1 0.6334、C3 0.5424（仍近随机） | 部分证伪：融合有真实增益但未达可用线（<R3 0.6068）；C3 患病定义差异方向无效；合并训练确实稀释源信号 |
| E6 深度学习直接分类 | 深度网络表示能力可突破？ | MLP（64,32，early-stopping，按类别样本量反比赋权）直接分类 LODO；α=1e-2 与宽网络(128,64) 敏感性 | 均值 0.5814（α1e-2 0.5819 / 宽 0.5850）vs 基线 0.5603（+0.021），< R4 DANN 0.5947 < R3 0.6068；C1 0.6852（C1 方向最高）、C2 0.5332（低于基线）、C3 0.5257 | 证伪（均值层面）：深度学习直接分类无效且不如域适应 DANN；宽网络不稳定（C1 0.4914 低于随机）；C1 方向 MLP 最强，坐实 CRC 方向存在可迁移信号 |

<!-- 队友写作：E0–E6 综合判断——六轮实验分别排除了「批次」「容量」「模型族」「评估方案」「模型组合方式」「深度学习表示能力」六类替代解释，跨疾病失败收敛为数据固有限制：疾病特异信号不迁移 + 患病定义差异。这是本问负结果（最优可达 0.6068 < 0.65）的因果闭环：跨疾病统一泛化不成立，且已证明不是模型选择问题。 -->

**疾病异质性**：跨疾病泛化失败并非方向对称——C1（CRC）存在弱可迁移信号（few-shot 后 0.6567，跨过可用线 0.65；MLP 0.6852 为 C1 方向最高），C2（IBD）不可学（0.5212），C3（Obesity）为患病定义差异（灵敏度 0.024→0.402 但 AUC 上限仍 0.59）。因此「三种疾病之间完全不存在可迁移信息」的表述不成立，准确表述是「统一跨疾病模型不可交付，泛化强度不足且方向高度异质」。

<!-- 队友写作：应用与改进——统一跨疾病模型不可交付；可保留「域内模型 + 跨疾病仅作参考」的应用方式；CRC 方向定向迁移（few-shot 0.66）是唯一可见的跨病增益点；改进方向为更大规模多疾病训练数据、目标疾病阈值对齐（标签定义统一）。 -->



综合以上结果，本问未交付可用的跨疾病预测模型。我们以"最优可达性能（R3，0.6068）加完整验证过程（四策略对比、候选模型均已尝试、衰减归因、方向一致性检验、阈值漂移量化）"的方式如实呈现。未来改进方向包括：引入更多疾病种类和更大样本量以增强共享信号；对目标疾病做无监督阈值重定以缓解基线漂移；探索疾病特异方向的结构化迁移而非简单特征共享；以及与 S2 筛选的生物标志物做交叉验证，寻找真正跨疾病保守的微生物信号。
