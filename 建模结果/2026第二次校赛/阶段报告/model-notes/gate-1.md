# 门禁记录：1 建模方案定稿（门禁 1）

> 日期：2026-08-21 | 运行模式：auto（goal：**自动模式直到模型决策裁决，裁决交给人类**）
> **本门禁 = goal 定义停止点**：方案探索 1.0-1.3 全部自动化完成，门禁 1（模型决策裁决）呈递人类拍板。

## 判定内容与审查结论（引用 review 结论文件原文）

| 子问题 | 审查结论文件 | 结论 | 问题清单 |
|---|---|---|---|
| S1 | `review-1.3-S1.md` | **通过**（有条件） | #1 中/B级：c-data-cleaned.pkl 不在 S1 worktree，1.4 前需 merge 赛题分支（**已处理**：三子问题 worktree 均已 merge `problems/2026-sim2-B`，pkl 已就位）；#2/#3/#4 低：符号 g_i 注明替换后、Chatelier class_weight 方向、LOOCV 耗时提示（不要求返工） |
| S2 | `review-1.3-S2.md` | **通过** | Q1 B级：clr_delta 占位符违反 No Placeholders（**已销项**：回填 6.5e-06，commit）；Q2 B级：FDR m 口径（全 1331 vs 稳定特征，1.4 前澄清）；Q3 B级：近全零过滤每病独立 vs 三病并集（1.4 前裁定） |
| S3 | `review-1.3-S3.md` | **通过** | #1 中/B级：A 类共享事实来自 B-raw.pkl，1.4 用 c-data-cleaned.pkl 复核（handoff 已注明）；#2/#3/#4 低：AUC 0.56 表述一致化、特征交集转导式边界声明、Platt A>0 校验 |

> 三份 review 均含：必读清单已读汇报 + 判定内容逐项结论（结论+依据+证据路径）+ 问题清单（问题|严重度|证据路径）+ 通过/不通过。**无空洞审查**（逐项结论与问题清单齐全）。

## 主建模自检核实（裁决前过 1-3 个关键点，非审查）

1. 三份 handoff frontmatter 抽查：handoff_type=code-agent、sub=S1/S2/S3、stage=1.3、source_docs 齐全、next_action 一致 ✓
2. 数据源核对：`c-data-cleaned.pkl`（484×1333，float32）实际存在（赛题 worktree 68d6121 产出）✓
3. 三方案口径一致性：CLR δ=6.5e-06 三题统一（S2 占位符已回填）✓

## 裁决（呈递人类拍板）

**goal 停止点：模型决策裁决交给人类。** 以下方案经 1.0-1.3 全自动流程产出（决策树 → A 类验证 → 辩论 → 确认书 → 审查），请人类确认：

| 子问题 | 推荐方案（待人类签字） |
|---|---|
| **S1 疾病预测模型** | 主：Logistic(L2)+CLR（δ=6.5e-06 乘法替换+几何均值中心化）+class_weight；对照：RF(原始丰度)；基线：单特征+Dummy；评估：分层 5 折 CV + AUC 主指标 + F1/Recall(少数类) + LOOCV 兜底；预期 AUC：Zeller ~0.81 / metahit ~0.89 / Chatelier ~0.64（A 类实测） |
| **S2 特征选择·标志物** | 主：稳定性选择（Lasso+bootstrap，τ=0.5）；前置：近全零过滤（>95%，1331→264）+ CLR；解释层：Wilcoxon+BH-FDR 两路信号（Fisher 存在/缺失 + 非零丰度）+ 共现分析；佐证：RF 重要性 + PLS-DA VIP>1.5；CV 折内选择防泄漏 |
| **S3 跨疾病预测** | 主框架：四策略正式对比（直接迁移/共享标志物/分类学聚合/部署校正）+ 紧急回退协议（R1-R4，可用线 ≥0.65）；性能探讨：衰减归因 + 深度迁移分析 + 阈值漂移量化 |

## 人类最终裁决（2026-08-21 逐项拍板，全部批准）

| 裁定 | 内容 | 落盘 |
|---|---|---|
| 特征过滤 | S1/S3 主口径统一「近全零过滤 1331→264 → CLR → L2」（全量降为对照） | S1/S3 `[门禁1微调]` |
| S2 共现分析 | Lasso 交互盲区 → 解释层加共现分析初探 + 边界声明 | S2 `[门禁1微调]` |
| S3 重构 [A级] | 策略对比=正式建模；全部失败→紧急回退重新选模型；负结果不是终点 | S3 `[门禁1重构]` |
| B1 | small_adenoma 四口径全做择优（归健康/归病变/剔除/单开一类），S2/S3 跟随 | S1 `[裁定落实]` |
| B2 | FDR m=1331 全特征规模校正（医疗宁可严格） | S2 `[裁定落实]` |
| B9 | 两基模型 AUC 均 ≥0.75 则做 Soft Voting | S1 `[裁定落实]` |
| B4/B5/B6 | 放行（判别标准+回滚方案充分） | S3 `[裁定落实B4B5B6]` |
| B7/B8/B10/B11 | 验证决定 | 已标记 |

**结论：门禁 1 通过（人类全部批准）**。下一门禁：汇聚门禁 M（1.4 预处理 + 2.0 推导）。

## 采用方案与下一步

人类签字后 → **三路并行开工**：
- S1/S2/S3 各开**代码对话**做 1.4 预处理（读各自 handoff，merge 已完成）
- 各开**建模对话**做 2.0 数学推导（math-<subN>.tex）
- 各开**报告对话**做 2.3 骨架段（门禁 1 后提前开工）

## 变更记录

| 时间 | 动作 |
|---|---|
| 2026-08-21 | 三子问题 worktree merge 赛题分支（c-data-cleaned.pkl 就位） |
| 2026-08-21 | S2 Q1 销项：clr_delta 回填 6.5e-06 |
| 2026-08-21 | **人类裁定（部分拍板）**：采纳特征过滤推荐——S1 主口径改「近全零过滤 1331→264 → CLR → L2」，全量降为对照；S3 主实验同步统一口径（S1: `[S1] 门禁1微调` commit；S3: `[S3] 门禁1微调` commit）。其余 B 组待裁定项（B1-B11）人类仍在审阅，**管线保持停止**，待人类审阅完毕再拍板剩余项并三路开工 |
| 2026-08-21 | **人类裁定（S2 补充）**：人类质询 Lasso 交互效应盲区 → 裁定采纳「共现分析」补充（S2 §1.3b：入选标志物两两 Spearman 相关 + Fisher 共现/互斥检验 + 网络 + 边界声明；S2 `[S2] 门禁1微调` commit）。**管线仍保持停止**，人类继续审阅 |
| 2026-08-21 | **人类裁定（S3 重构 [A级]）**：人类指出 S3「接受负结果」框架在逃避正面回答问题 → 裁定：①策略对比工作升级为**正式建模部分**；②若所有策略都不好用 → **紧急回退重新选模型**，负结果不是合法终点。已重构 S3（approach §1.1-1.6：四策略正式对比 + 回退协议 R1-R4 + 可用线 ≥0.65/提升 ≥0.10 + 穷尽证据链出口；S3 `[S3] 门禁1重构` commit）。**管线仍保持停止**，人类继续审阅 |
| 2026-08-21 | **人类裁定（B1/B2/B9 等，批量拍板）**：B1 small_adenoma **四口径全做择优**（归健康/归病变/剔除/单开一类，S1 实现，S2/S3 跟随主口径）；B2 FDR **m=1331 全特征规模校正**（医疗宁可严格不可虚报，S2 实现）；B9 **两基模型 AUC 均 ≥0.75 则做 Soft Voting**（条件化正式纳入，S1 实现）；B3/B4/B7/B8/B10/B11 等不确定项**验证决定**，其余采纳拟议。S1 `[S1] 门禁1裁定落实`、S2 `[S2] 门禁1裁定落实` commits。**管线仍保持停止**，人类继续审阅 |
| 2026-08-21 | **人类裁定（B4/B5/B6 放行）**：低风险拟议具充分判别标准与回滚方案 → 放行。B4：AUC 0.556 统一表述「接近随机」（判别标准：<0.60 接近随机，modeling-decision-tree 基准；回滚：正式实现后按 S3-results.pkl 实际数字定性）；B5：特征交集转导式边界显式声明（判别标准：仅特征存在性、绝不用测试集标签；回滚：若审查发现泄漏则该策略降级为参考，主结论不受影响）；B6：Platt 实现校验 A>0（判别标准：A≤0 触发警告并检查训练；回滚：A≤0 时不采用校准结果，仅报原始阈值迁移）。S3 `[S3] 门禁1裁定落实B4B5B6` commit。**管线仍保持停止**，人类继续审阅 |
