# 门禁记录：0.3 基础清洗（阶段 0 确认点 2/2）

> 日期：2026-08-21 | 运行模式：auto（goal：自动模式直到模型决策裁决，裁决交给人类）
> 按自动模式协议：0.3 确认点按推荐方案执行，登记待裁定项，人类在门禁 1 批量复核。

## 判定内容（对照 TRAE-建模.md 0.3 门禁：数据方案 + 清洗策略合并呈递）

1. **数据方案（0.2 盘点结果）**：
   - 484 样本 × 1331 特征（物种级相对丰度），92.21% 零值稀疏，定和成分数据（每行和=100）
   - 三数据集：Chatelier Obesity 253（患病 164/健康 89）、Zeller CRC 121（48/73）、metahit IBD 110（25/85）
   - 无重复行、无 NaN；特征域：Bacteria 1279 / Eukaryota 42 / Archaea 10
2. **清洗策略（8 条口径）**：重复检测断言、零值不填补（真实稀疏）、NaN 仅报告、float32 压缩、无变换（推迟 1.4）、无领域排除（small_adenoma 保留）、落盘 `c-data-cleaned.pkl`、清洗报告
3. **两遍审核结论**：
   - ① 口径审查（建模子代理）：**通过**——8 条清洗策略逐项一致（`review-0.3-口径.md`）
   - ② 实现审查（代码子代理）：**通过**——8 检查项全过（`review-0.3-实现.md`）
   - 3 项低严重度提示已修复并复审（注释歧义、feat_cols 断言、读回验证）

## 裁决（自动模式，默认按推荐）

- 采纳清洗方案，`c-data-cleaned.pkl` 作为全部子问题共享数据基线
- 数据注释区已填充 `domain-knowledge.md`（列含义/标签映射/清洗决策）

## 待裁定项登记

- [B级] small_adenoma 口径（26 例归健康对照）——已登记 `maintenance/registry.md`，S1 做敏感性分析（剔除重训对比 AUC）
- [B级] 类别不平衡：metahit 患病仅 25 例（ibd_crohn_disease 仅 4 例）——S1 方案需显式处理（类别权重/评估指标），门禁 1 裁定

## 自检核实记录

- 盘点数字 ↔ 清洗报告一致：shape 484×1333、重复 0、NaN 0 ✓
- review 结论文件逐项结论齐全（非空洞审查）✓
- 0.4 数据画像已并行启动（自动通过）✓
